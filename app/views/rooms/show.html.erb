<p id="notice"><%= notice %></p>

<p>
  <strong>Spacename:</strong>
  <%= @room.spacename %>
</p>

<p>
  <strong>Spacenumber:</strong>
  <%= @room.spacenumber %>
</p>

<p>
  <strong>Area:</strong>
  <%= @room.area %>
</p>

<h2>Equipments</h2>
<table>
<tr>
 <th>Tag</th>
 <th>Name</td>
 <th>Quantity</th>
</tr>


<% @room.room_equipments.each do |eq| %>
<% @equipment = Equipment.find(eq.tag) %>
<tr> 
 <td><%= @equipment.tag %></td>
 <td><%= @equipment.name %></td>
 <td><%= eq.quantity %></td>
</tr>

<% end %>

</table>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>
